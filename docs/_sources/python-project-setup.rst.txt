Python Project Setup
####################

This project uses }TDD| to make sure everything works. Testing is managed locally using tox_ and pytest_. In addition, the project is analyzed on several public |CI| servers; travis_ci, appveyor_ and
