language: python

python:
  - "3.9"

install:
  - pip install --upgrade pip
  - pip install -r requirements.txt
  - sudo add-apt-repository -y ppa:openscad/releases
  - sudo apt update
  - sudo apt install openscad=2019.05-3~19.10

script:
    python -m pytest

after_success:
  - coveralls
