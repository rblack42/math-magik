language: python

branches:
    only:
        - master

python:
    - "3.9"

install:
    - pip install -r requirements.txt
    - pip install coveralls

script:
    - pytest --cov=mmdesigner --cov-report term
    - coveralls
