[general]
name = mmdesigner

[tox]
envlist = py39

[testenv]

deps:
    pytest
    pytest-cov
    flake8

commands =
    pytest \
        --cov-report=term-missing \
        --cov-report=xml \
        -cov=mmdesigner
    flake8 setup.py mmdesigner
